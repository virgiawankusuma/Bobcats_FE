import { NoSpacingPipe } from './no-spacing.pipe';

describe('NoSpacingPipe', () => {
  it('create an instance', () => {
    const pipe = new NoSpacingPipe();
    expect(pipe).toBeTruthy();
  });
});
